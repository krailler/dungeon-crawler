{
  "name": "dungeon-crawler",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@dungeon/client",
    "build": "npm run build --workspace=@dungeon/client",
    "server": "npm run start --workspace=@dungeon/server",
    "server:dev": "npm run dev --workspace=@dungeon/server",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:generate": "npm run db:generate --workspace=@dungeon/server",
    "db:migrate": "npm run db:migrate --workspace=@dungeon/server",
    "db:seed": "npm run seed --workspace=@dungeon/server",
    "db:studio": "npm run db:studio --workspace=@dungeon/server",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,json,md,html,css}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3",
    "prettier": "^3.8.1",
    "typescript": "~5.9.3"
  }
}
