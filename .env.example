# PostgreSQL connection string
DATABASE_URL=postgres://dungeon:dungeon@localhost:5432/dungeon

# JWT secret (change in production)
JWT_SECRET=dungeon-dev-secret-change-in-prod

# Express session secret (used by Colyseus OAuth middleware)
SESSION_SECRET=dungeon-dev-session-secret-change-in-prod

# Server port
PORT=3000
