/** Maximum level difference between players in a quick play group */
export const MATCHMAKING_LEVEL_RANGE = 6; // ±3 levels

/** Minimum players required to form a match */
export const MATCHMAKING_MIN_PLAYERS = 2;

/** Maximum players in a matched group (same as dungeon max) */
export const MATCHMAKING_MAX_PLAYERS = 4;

/** How often the server tries to form groups (ms) */
export const MATCHMAKING_TICK_INTERVAL = 2000;
