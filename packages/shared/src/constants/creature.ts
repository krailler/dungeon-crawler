// Creature — stats are defined in DB (world.creatures)
export const CREATURE_REPATH_INTERVAL = 0.5;
/** Distance threshold for creatures to snap to a waypoint (larger to avoid jitter) */
export const CREATURE_WAYPOINT_THRESHOLD = 0.15;
