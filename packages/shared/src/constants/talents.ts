/** Minimum character level to start earning talent points */
export const TALENT_UNLOCK_LEVEL = 5;

/** Gold cost per player level to reset talents (cost = level × this value) */
export const TALENT_RESET_GOLD_PER_LEVEL = 10;

/** Gold cost per player level to reset stat allocations (cost = level × this value) */
export const STAT_RESET_GOLD_PER_LEVEL = 10;
